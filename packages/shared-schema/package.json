{
  "name": "@weyos/shared-schema",
  "version": "1.0.0",
  "private": true,
  "description": "The Weyos contract. JSON Schema is the source of truth; TS types are its projection.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src", "schemas"],
  "scripts": {
    "generate": "json-schema-to-typescript -i 'schemas/*.schema.json' -o src/generated.ts --bannerComment '// GENERATED FILE — do not edit. Run npm run generate.'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0",
    "typescript": "^5.6.0"
  }
}
